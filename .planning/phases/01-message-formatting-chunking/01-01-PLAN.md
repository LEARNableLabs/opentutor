---
phase: 01-message-formatting-chunking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/tutor/references/lesson-delivery.md
autonomous: true
requirements:
  - FMT-01
  - FMT-03
must_haves:
  truths:
    - "lesson-delivery.md contains a Message Chunking section with rules for splitting lessons into 3-4 messages"
    - "lesson-delivery.md defines 4 emoji anchors (book, brain, lightbulb, pencil) as structural markers"
    - "lesson-delivery.md contains per-format message sequence templates for all 5 format variations"
    - "lesson-delivery.md specifies 150-word soft cap with semantic splitting rules"
    - "lesson-delivery.md requires every lesson's final message to end with an engagement prompt"
    - "lesson-delivery.md requires minimum 2 messages per lesson"
  artifacts:
    - path: "skills/tutor/references/lesson-delivery.md"
      provides: "Universal chunking rules, emoji anchor system, per-format message sequences"
      contains: "## Message Chunking"
  key_links:
    - from: "skills/tutor/references/lesson-delivery.md"
      to: "skills/tutor/SKILL.md"
      via: "referenced from SKILL.md Lesson Flow section"
      pattern: "lesson-delivery.md"
---

<objective>
Add universal message chunking rules, emoji anchor definitions, and per-format message sequence templates to `skills/tutor/references/lesson-delivery.md`.

Purpose: This is the foundational skill file update. All platforms inherit formatting from lesson-delivery.md (per D-05). The chunking rules, anchor system, and message sequences defined here become the source of truth for how every lesson is structured and delivered across all platforms.

Output: Updated `skills/tutor/references/lesson-delivery.md` with three new sections: Message Chunking, Emoji Anchors, and Message Sequence Templates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-message-formatting-chunking/01-CONTEXT.md
@.planning/phases/01-message-formatting-chunking/01-RESEARCH.md
@.planning/phases/01-message-formatting-chunking/01-UI-SPEC.md

<interfaces>
<!-- Current state of the file being modified. Executor MUST read before editing. -->

From skills/tutor/references/lesson-delivery.md:
- Sections: Daily Delivery Modes, Lesson Structure, Format Variations, Review Days, Weekly Summary, Resources & Research, Formatting
- Line 96: "Emoji sparingly as visual anchors (one or two per message, not every bullet)" -- MUST BE REPLACED by new anchor system
- Line 97: "Keep each message focused -- if a lesson has multiple parts, send multiple short messages rather than one giant one" -- MUST BE EXPANDED with chunking rules
- Format Variations (line 42-49): Lists 5 formats (mini-lesson, question, resource drop, teach-back, real-world challenge) -- each needs a message sequence template added

From skills/tutor/SKILL.md (line 63):
- "Emoji sparingly (1-2 per message) as visual anchors" -- will need update in Plan 02
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Message Chunking and Emoji Anchor sections to lesson-delivery.md</name>
  <files>skills/tutor/references/lesson-delivery.md</files>
  <read_first>
    - skills/tutor/references/lesson-delivery.md
    - .planning/phases/01-message-formatting-chunking/01-CONTEXT.md
    - .planning/phases/01-message-formatting-chunking/01-UI-SPEC.md
  </read_first>
  <action>
Update `skills/tutor/references/lesson-delivery.md` with two new sections inserted AFTER the existing "Lesson Structure" section (after line 38) and BEFORE "Format Variations" (line 40). Also update the existing "Formatting" section at the bottom of the file.

**Section 1: Add "## Message Chunking" after "## Lesson Structure"**

Content to add (verbatim):

```
## Message Chunking

Every lesson is delivered as a sequence of short messages, not a single wall of text. Each message focuses on one pedagogical section.

### Splitting Rules

- **Split by pedagogical section** — each section (hook, concept, example, exercise) becomes its own message. Never split by arbitrary word count or fixed template.
- **Soft cap: ~150 words per message** — aim for 150 words, but finishing a thought clearly is more important than hitting a number. Exceeding to ~200 words is fine if splitting mid-thought would hurt clarity.
- **Minimum 2 messages per lesson** — every lesson must feel multi-message. Even simple formats ("just a question") get at least 2 messages.
- **Target 3-4 messages for standard lessons** — mini-lessons typically use 4 messages (hook, concept, example, exercise). Other formats use 2-3.

### Overflow Handling

When a section exceeds the soft cap:

1. Check if there's a natural paragraph boundary to split at
2. If yes — split into two messages with the same emoji anchor
3. If no — keep as one message (clarity over strict limits)
4. **Exception:** Code blocks are always kept whole, even if they exceed 150 words. Splitting code breaks syntax. Code blocks are visually distinct and don't create "wall of text" fatigue.

### Engagement Rule

Every lesson's final message must end with an engagement prompt — an exercise, a question, or a teaser for tomorrow. Never end with "That's it for today!" The conversation loop must stay open.
```

**Section 2: Add "## Emoji Anchors" after "## Message Chunking"**

Content to add (verbatim):

```
## Emoji Anchors

Four emoji are used as structural anchors at the start of each message. They signal content type, not decoration.

| Emoji | Role | When to Use |
|-------|------|-------------|
| book emoji | Title / progress | First message of every lesson. Format: "book emoji Day N/total - Topic -- callback. Today: teaser?" |
| brain emoji | Core concept | Concept explanation messages. Definitions, principles, key ideas. |
| lightbulb emoji | Example | Examples, analogies, real-world illustrations. |
| pencil emoji | Exercise | Exercises, problems, engagement prompts. |

### Rules

- **One anchor per message** — place the emoji at the very start of the message, before any text
- **No decorative emoji** — do not use party, sparkles, fire, or other emoji for personality. Personality comes from text, not emoji
- **Consistent meaning** — the same emoji always means the same thing. Students learn to scan by anchor
- **Every message gets an anchor** — no lesson message should lack its structural emoji
```

NOTE: In the actual file, use the real emoji characters: use the actual book emoji character (the one that looks like an open book with pages), brain emoji, lightbulb emoji, and pencil emoji. The table and rules text above uses placeholder words because this plan is plain text. The exact emoji characters are: the open book (U+1F4D6 for the title role -- but per D-04 use U+1F4D6), the brain (U+1F9E0), the lightbulb (U+1F4A1), and the pencil (U+270F). IMPORTANT CORRECTION: Per D-04 from CONTEXT.md, the exact emoji are:
- Title/progress: 📖 (U+1F4D6)
- Core concept: 🧠 (U+1F9E0)
- Example: 💡 (U+1F4A1)
- Exercise: ✏️ (U+270F+FE0F)

Use these exact emoji characters in the file.

**Section 3: Update existing "## Formatting" section (currently the last section, starting around line 86)**

Replace the current "## Formatting" section entirely with:

```
## Formatting

Lessons are delivered via messaging channels (Slack, Telegram, WhatsApp, etc.). Keep formatting readable across platforms:

- **Scannable** — bullets and short paragraphs, no walls of text
- **Bold** for key terms and definitions
- _Italic_ for emphasis and variables
- `code` for math, formulas, inline code, and symbols
- `code blocks` for longer code or worked examples
- Block quotes for key definitions or takeaways
- **No tables** — use bullet lists instead (tables render poorly on mobile messaging apps)
- **No headers** — use **bold** or emoji anchors as section markers
- Keep each message focused on one section — use the chunking rules above
- Follow the emoji anchor system — one structural emoji per message (see Emoji Anchors section above)
```

This replaces the old line "Emoji sparingly as visual anchors (one or two per message, not every bullet). Match the student's emoji style -- if they use them, mirror; if not, keep minimal" with the structured anchor reference.
  </action>
  <verify>
    <automated>grep -c "## Message Chunking" skills/tutor/references/lesson-delivery.md && grep -c "## Emoji Anchors" skills/tutor/references/lesson-delivery.md && grep -c "150 words" skills/tutor/references/lesson-delivery.md && grep -c "Minimum 2 messages" skills/tutor/references/lesson-delivery.md && grep "📖" skills/tutor/references/lesson-delivery.md | head -3 && grep "🧠" skills/tutor/references/lesson-delivery.md | head -3 && grep "💡" skills/tutor/references/lesson-delivery.md | head -3 && grep "✏️" skills/tutor/references/lesson-delivery.md | head -3 && grep -c "engagement prompt" skills/tutor/references/lesson-delivery.md && grep -c "No decorative emoji" skills/tutor/references/lesson-delivery.md</automated>
  </verify>
  <acceptance_criteria>
    - lesson-delivery.md contains section header "## Message Chunking"
    - lesson-delivery.md contains section header "## Emoji Anchors"
    - lesson-delivery.md contains the string "Soft cap: ~150 words per message"
    - lesson-delivery.md contains the string "Minimum 2 messages per lesson"
    - lesson-delivery.md contains the string "Target 3-4 messages for standard lessons"
    - lesson-delivery.md contains the string "Code blocks are always kept whole"
    - lesson-delivery.md contains the string "engagement prompt" at least twice (once in chunking, once in formatting or sequences)
    - lesson-delivery.md contains all 4 emoji characters: 📖 🧠 💡 ✏️
    - lesson-delivery.md contains the string "No decorative emoji"
    - lesson-delivery.md contains the string "One anchor per message"
    - lesson-delivery.md does NOT contain the old string "Emoji sparingly as visual anchors"
    - lesson-delivery.md does NOT contain the old emoji 📌 or 🔬 or ⚡
  </acceptance_criteria>
  <done>
    lesson-delivery.md has Message Chunking section with splitting rules (pedagogical sections, 150-word soft cap, minimum 2 messages, 3-4 target, overflow handling, code block exception, engagement rule) and Emoji Anchors section with 4-anchor table (📖🧠💡✏️), rules (one per message, no decorative, consistent meaning), and updated Formatting section referencing the new systems. Old "emoji sparingly" guidance is removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add per-format message sequence templates to lesson-delivery.md</name>
  <files>skills/tutor/references/lesson-delivery.md</files>
  <read_first>
    - skills/tutor/references/lesson-delivery.md
    - .planning/phases/01-message-formatting-chunking/01-CONTEXT.md
    - .planning/phases/01-message-formatting-chunking/01-UI-SPEC.md
  </read_first>
  <action>
Update the existing "## Format Variations" section in `skills/tutor/references/lesson-delivery.md`. The current section (around line 40) lists 5 formats as a bullet list. Replace the entire "## Format Variations" section with the expanded version below that adds message sequence templates for each format.

Replace the current "## Format Variations" section with:

```
## Format Variations

Don't deliver the same style every time. Rotate through these formats. Each has its own message sequence template.

### Mini-Lesson (4 messages)

The most common format. Concept + example + exercise.

**Sequence:** 📖 Hook → 🧠 Concept → 💡 Example → ✏️ Exercise

- **Message 1 (📖 Hook):** Title, day progress, callback to yesterday's lesson, teaser for today's topic. Format: "📖 Day N/total · Topic — Yesterday we [callback]. Today: [teaser]?"
- **Message 2 (🧠 Concept):** Core definition or principle. 1-3 short paragraphs. Bold key terms. Keep under 150 words.
- **Message 3 (💡 Example):** Concrete analogy or real-world illustration. Make abstract ideas tangible.
- **Message 4 (✏️ Exercise):** Problem, question, or prompt. Must invite a reply. Format: "✏️ [problem] — [instruction]. Reply with your answer."

### Just a Question (2 messages)

A thought-provoking question to chew on. Low-pressure, high-curiosity.

**Sequence:** 📖 Hook → 🧠 Question

- **Message 1 (📖 Hook):** "📖 Day N/total — Quick one today..."
- **Message 2 (🧠 Question):** "🧠 Think about this: [thought-provoking question]?" The question should be open-ended and connect to upcoming material.

### Resource Drop (3 messages)

Share a video, article, book, or tool. Give context for why it matters.

**Sequence:** 📖 Hook → 💡 Resource → ✏️ Follow-up

- **Message 1 (📖 Hook):** "📖 Day N/total — Time for a different perspective..."
- **Message 2 (💡 Resource):** "💡 [resource description]: [link]. [Why it's valuable — 1-2 sentences]."
- **Message 3 (✏️ Follow-up):** "✏️ After [engaging with resource], tell me: [reflection question]?"

### Teach-Back (3 messages)

Student explains a concept in their own words. Tests deep understanding.

**Sequence:** 📖 Hook → ✏️ Prompt → 🧠 Scaffolding

- **Message 1 (📖 Hook):** "📖 Day N/total — Let's see if you can explain this..."
- **Message 2 (✏️ Prompt):** "✏️ Explain [concept] in your own words. Pretend you're teaching it to someone who's never heard of it."
- **Message 3 (🧠 Scaffolding):** "🧠 Hint: Start with [anchor point]. Then connect it to [related concept from previous lesson]."

### Real-World Challenge (3 messages)

Hands-on problem. Coding exercise, calculation, or practical application.

**Sequence:** 📖 Hook → ✏️ Challenge → 💡 Hint

- **Message 1 (📖 Hook):** "📖 Day N/total — Ready for a tricky one?"
- **Message 2 (✏️ Challenge):** "✏️ [hard problem statement]. Take your time."
- **Message 3 (💡 Hint):** "💡 Stuck? Think about [hint]. Or reply 'skip' and I'll show you the solution."

### Rules for All Formats

- **Final message must have an engagement prompt** — exercise, question, or teaser for tomorrow. No dead-end messages.
- **Minimum 2 messages** — even the shortest format (question) uses 2 messages.
- **Each message gets one emoji anchor** — placed at the start, signaling content type.
- **Adapt, don't rigidly follow** — these are templates, not scripts. If the content naturally calls for an extra message or a different anchor order, that's fine. The spirit is structured variety, not mechanical repetition.
```

This replaces the old 5-line bullet list with full sequence templates per D-06, D-07, and D-08.
  </action>
  <verify>
    <automated>grep -c "### Mini-Lesson" skills/tutor/references/lesson-delivery.md && grep -c "### Just a Question" skills/tutor/references/lesson-delivery.md && grep -c "### Resource Drop" skills/tutor/references/lesson-delivery.md && grep -c "### Teach-Back" skills/tutor/references/lesson-delivery.md && grep -c "### Real-World Challenge" skills/tutor/references/lesson-delivery.md && grep -c "### Rules for All Formats" skills/tutor/references/lesson-delivery.md && grep "Hook.*Concept.*Example.*Exercise" skills/tutor/references/lesson-delivery.md | head -1 && grep -c "Final message must have an engagement prompt" skills/tutor/references/lesson-delivery.md</automated>
  </verify>
  <acceptance_criteria>
    - lesson-delivery.md contains "### Mini-Lesson (4 messages)" with sequence "📖 Hook -> 🧠 Concept -> 💡 Example -> ✏️ Exercise"
    - lesson-delivery.md contains "### Just a Question (2 messages)" with sequence "📖 Hook -> 🧠 Question"
    - lesson-delivery.md contains "### Resource Drop (3 messages)" with sequence "📖 Hook -> 💡 Resource -> ✏️ Follow-up"
    - lesson-delivery.md contains "### Teach-Back (3 messages)" with sequence "📖 Hook -> ✏️ Prompt -> 🧠 Scaffolding"
    - lesson-delivery.md contains "### Real-World Challenge (3 messages)" with sequence "📖 Hook -> ✏️ Challenge -> 💡 Hint"
    - lesson-delivery.md contains "### Rules for All Formats" section
    - lesson-delivery.md contains the string "Final message must have an engagement prompt"
    - lesson-delivery.md contains the string "Minimum 2 messages"
    - lesson-delivery.md contains "Day N/total" pattern in at least 3 format templates
    - lesson-delivery.md does NOT contain the old bullet-only format list without sequence templates
  </acceptance_criteria>
  <done>
    All 5 format variations in lesson-delivery.md have per-format message sequence templates with emoji anchors, message counts, and example text. Rules for All Formats section establishes universal constraints (engagement prompt, minimum 2 messages, one anchor per message, adaptation flexibility). The old bullet-list-only format section is replaced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill file -> Agent runtime | Agent interprets prose instructions at runtime. Malformed instructions could cause unexpected behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | lesson-delivery.md | accept | File is in version control (git). Changes are reviewed via diff. No user input processed at this layer -- these are static instructions read by the agent. Risk is minimal. |
| T-01-02 | Information Disclosure | lesson-delivery.md | accept | File contains no secrets, no PII, no credentials. It is a public-facing pedagogical reference. No mitigation needed. |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c "## Message Chunking" skills/tutor/references/lesson-delivery.md` returns 1
2. `grep -c "## Emoji Anchors" skills/tutor/references/lesson-delivery.md` returns 1
3. `grep -c "### Mini-Lesson" skills/tutor/references/lesson-delivery.md` returns 1
4. `grep -c "### Just a Question" skills/tutor/references/lesson-delivery.md` returns 1
5. `grep -c "### Resource Drop" skills/tutor/references/lesson-delivery.md` returns 1
6. `grep -c "### Teach-Back" skills/tutor/references/lesson-delivery.md` returns 1
7. `grep -c "### Real-World Challenge" skills/tutor/references/lesson-delivery.md` returns 1
8. All 4 emoji anchors present: 📖 🧠 💡 ✏️
9. Old "Emoji sparingly" guidance removed
10. Old bullet-only format list replaced with sequence templates
</verification>

<success_criteria>
- lesson-delivery.md is the single source of truth for message chunking rules (D-01, D-02, D-03), emoji anchors (D-04, D-05), and per-format message sequences (D-06, D-07, D-08)
- All content is platform-agnostic (no Telegram-specific instructions per D-05)
- Old conflicting guidance ("emoji sparingly", 6-anchor system references) is removed
- File is well-structured with clear section hierarchy readable by both humans and agents
</success_criteria>

<output>
After completion, create `.planning/phases/01-message-formatting-chunking/01-01-SUMMARY.md`
</output>
