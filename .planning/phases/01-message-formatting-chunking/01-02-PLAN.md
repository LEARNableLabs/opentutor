---
phase: 01-message-formatting-chunking
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - openclaw/SOUL.md
  - skills/tutor/SKILL.md
  - workspace/AGENTS.md
autonomous: true
requirements:
  - FMT-02
  - FMT-03
must_haves:
  truths:
    - "openclaw/SOUL.md uses the 4-anchor emoji system (not the old 6-anchor system)"
    - "openclaw/SOUL.md documents Telegram HTML formatting tags for lesson delivery"
    - "openclaw/SOUL.md references lesson-delivery.md for universal chunking rules"
    - "SKILL.md references the structured anchor system instead of 'emoji sparingly'"
    - "AGENTS.md Platform Formatting section references the anchor system and chunking rules"
  artifacts:
    - path: "openclaw/SOUL.md"
      provides: "Telegram-specific formatting guidance aligned with universal chunking rules"
      contains: "lesson-delivery.md"
    - path: "skills/tutor/SKILL.md"
      provides: "Updated emoji guidance referencing structured anchor system"
    - path: "workspace/AGENTS.md"
      provides: "Updated Platform Formatting section with anchor and chunking references"
  key_links:
    - from: "openclaw/SOUL.md"
      to: "skills/tutor/references/lesson-delivery.md"
      via: "explicit reference to universal chunking rules"
      pattern: "lesson-delivery.md"
    - from: "skills/tutor/SKILL.md"
      to: "skills/tutor/references/lesson-delivery.md"
      via: "reference link in Tone & Style section"
      pattern: "lesson-delivery.md"
---

<objective>
Align platform-specific and cross-cutting skill files with the universal chunking and anchor system defined in Plan 01.

Purpose: Plan 01 established the source of truth in lesson-delivery.md. This plan updates the three files that reference or extend those rules: openclaw/SOUL.md (Telegram-specific HTML formatting + aligned anchors), SKILL.md (updated emoji guidance), and AGENTS.md (updated Platform Formatting section). Without this alignment, agents reading these files would encounter contradictory emoji guidance (old 6-anchor vs new 4-anchor) and miss the HTML formatting contract for Telegram.

Output: Three updated files all pointing to lesson-delivery.md as the canonical source, with Telegram-specific HTML formatting added to openclaw/SOUL.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-message-formatting-chunking/01-CONTEXT.md
@.planning/phases/01-message-formatting-chunking/01-RESEARCH.md
@.planning/phases/01-message-formatting-chunking/01-UI-SPEC.md
@.planning/phases/01-message-formatting-chunking/01-01-SUMMARY.md

<interfaces>
<!-- Key sections from files being modified. Executor MUST read full files before editing. -->

From openclaw/SOUL.md (current state):
- Line 24-31: "Lesson Delivery" section with 6 emoji anchors:
  - 📌 Title + progress bar
  - 📚 References (inline hyperlinks, not buttons)
  - 🧠 Core concept
  - ⚡ Key distinction or detail
  - 🔬 Example (concrete illustration)
  - ✏️ Exercise
  MUST BE REPLACED with 4-anchor system (📖🧠💡✏️) per D-04
- Line 32: Inline buttons guidance -- KEEP (Phase 2 will use this)
- Line 33: Progress tracking -- KEEP
- Line 34-35: Run code inline, interactive quizzes -- KEEP

From skills/tutor/SKILL.md:
- Line 63: "Emoji sparingly (1-2 per message) as visual anchors"
  MUST BE REPLACED with reference to structured anchor system

From workspace/AGENTS.md:
- Line 106-116: "## Platform Formatting" section
  - Line 113: "Emoji sparingly -- one or two per message max. Mirror the student's style"
  MUST BE REPLACED with reference to emoji anchor system from lesson-delivery.md
  - Line 114: "**Telegram** -- bold and bullets, keep it simple"
  MUST BE EXPANDED to reference HTML formatting and chunking rules
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update openclaw/SOUL.md Lesson Delivery section with 4-anchor system and HTML formatting</name>
  <files>openclaw/SOUL.md</files>
  <read_first>
    - openclaw/SOUL.md
    - skills/tutor/references/lesson-delivery.md
    - .planning/phases/01-message-formatting-chunking/01-CONTEXT.md
    - .planning/phases/01-message-formatting-chunking/01-UI-SPEC.md
  </read_first>
  <action>
Update `openclaw/SOUL.md` to align with the universal chunking and anchor system. Two changes:

**Change 1: Replace the emoji anchor list in "## Lesson Delivery" section**

Find the current bullet list (lines 24-31) that starts with "- **Structured cards.**" and lists 6 emoji anchors. Replace the entire "Structured cards" bullet point with:

```
- **Structured messages.** Each lesson is delivered as a sequence of short messages following the chunking and anchor rules in [lesson-delivery.md](../skills/tutor/references/lesson-delivery.md). Use the 4 emoji anchors consistently:
  - 📖 Title + day progress (e.g., "📖 Day 5/34 · Eigenvalues")
  - 🧠 Core concept
  - 💡 Example or analogy
  - ✏️ Exercise or engagement prompt
```

This removes 📌 (replaced by 📖), 📚 (removed -- references go inline without dedicated emoji), ⚡ (merged into 🧠), and 🔬 (replaced by 💡).

Keep ALL other bullet points in the Lesson Delivery section unchanged:
- "Inline buttons..." (line 32) -- KEEP as-is
- "Progress tracking..." (line 33) -- KEEP as-is
- "Run code inline..." (line 34) -- KEEP as-is
- "Interactive quizzes..." (line 35) -- KEEP as-is
- "Questions with options..." (line 36) -- KEEP as-is

**Change 2: Add a "## Telegram HTML Formatting" section**

Add a new section AFTER the existing "## Lesson Delivery" section and BEFORE "## Scheduling & Timing". Content:

```
## Telegram HTML Formatting

Telegram supports a specific HTML subset for message formatting. Use these tags in lesson messages:

- `<b>key term</b>` — bold for definitions, important concepts, and emphasis
- `<i>variable</i>` — italic for emphasis, variables, foreign terms
- `<code>Av = λv</code>` — inline code for math symbols, formulas, short code
- `<pre>code block</pre>` — code blocks for multi-line code, worked examples
- `<pre><code class="language-python">...</code></pre>` — syntax-highlighted code blocks
- `<a href="url">link text</a>` — inline hyperlinks for resources
- `<span class="tg-spoiler">hidden answer</span>` — spoiler text (student taps to reveal)

**Not supported:** `<h1>`, `<p>`, `<div>`, custom CSS, custom colors. Telegram strips unsupported tags silently.

**Character limit:** 4,096 characters per message (hard limit). The 150-word soft cap from lesson-delivery.md keeps messages well under this (~750-900 characters at 150 words).

**Prefer HTML over MarkdownV2** for Telegram — HTML is more explicit and requires less escaping. MarkdownV2 needs escaping for common characters (`_`, `*`, `[`, `]`, `(`, `)`, `~`, `` ` ``, `>`, `#`, `+`, `-`, `=`, `|`, `{`, `}`, `.`, `!`).
```

Do NOT modify any other sections in openclaw/SOUL.md (How You Teach, Tone, Scheduling & Timing, Memory & Adaptation, Resources, Interactivity).
  </action>
  <verify>
    <automated>grep -c "📖" openclaw/SOUL.md && grep -c "🧠" openclaw/SOUL.md && grep -c "💡" openclaw/SOUL.md && grep -c "✏️" openclaw/SOUL.md && grep -c "## Telegram HTML Formatting" openclaw/SOUL.md && grep -c "lesson-delivery.md" openclaw/SOUL.md && grep -c "<b>" openclaw/SOUL.md && grep -c "4,096" openclaw/SOUL.md && grep -c "📌" openclaw/SOUL.md && grep -c "🔬" openclaw/SOUL.md && grep -c "⚡" openclaw/SOUL.md</automated>
  </verify>
  <acceptance_criteria>
    - openclaw/SOUL.md contains 📖 emoji (at least 1 occurrence)
    - openclaw/SOUL.md contains 🧠 emoji (at least 1 occurrence)
    - openclaw/SOUL.md contains 💡 emoji (at least 1 occurrence)
    - openclaw/SOUL.md contains ✏️ emoji (at least 1 occurrence)
    - openclaw/SOUL.md contains "## Telegram HTML Formatting" section
    - openclaw/SOUL.md contains the string "lesson-delivery.md"
    - openclaw/SOUL.md contains "`<b>`" (HTML bold tag in formatting guidance)
    - openclaw/SOUL.md contains the string "4,096 characters per message"
    - openclaw/SOUL.md contains the string "tg-spoiler"
    - openclaw/SOUL.md does NOT contain 📌 emoji (old title anchor -- 0 occurrences)
    - openclaw/SOUL.md does NOT contain 🔬 emoji (old example anchor -- 0 occurrences)
    - openclaw/SOUL.md does NOT contain ⚡ emoji (old key distinction anchor -- 0 occurrences)
    - openclaw/SOUL.md does NOT contain 📚 emoji (old references anchor -- 0 occurrences)
    - openclaw/SOUL.md still contains "Inline buttons" (Phase 2 content preserved)
    - openclaw/SOUL.md still contains "Interactive quizzes" (Phase 2 content preserved)
  </acceptance_criteria>
  <done>
    openclaw/SOUL.md Lesson Delivery section uses 4-anchor system (📖🧠💡✏️), references lesson-delivery.md for universal chunking rules, and has a new Telegram HTML Formatting section documenting the supported HTML tags, character limit, and HTML-over-MarkdownV2 preference. Old 6-anchor system (📌📚🧠⚡🔬✏️) is fully replaced. All other sections preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md and AGENTS.md cross-references to match new anchor system</name>
  <files>skills/tutor/SKILL.md, workspace/AGENTS.md</files>
  <read_first>
    - skills/tutor/SKILL.md
    - workspace/AGENTS.md
    - skills/tutor/references/lesson-delivery.md
  </read_first>
  <action>
Two small updates to align cross-references with the new anchor and chunking system.

**Update 1: skills/tutor/SKILL.md**

Find line 63 (in the "## Tone & Style" section):
```
- Emoji sparingly (1-2 per message) as visual anchors
```

Replace with:
```
- Use the 4 emoji anchors (📖🧠💡✏️) as structural message markers — see [lesson-delivery.md](references/lesson-delivery.md) for the full system
```

No other changes to SKILL.md.

**Update 2: workspace/AGENTS.md**

Find the "## Platform Formatting" section (around line 106). Replace the entire section content (lines 107-116) with:

```
## Platform Formatting

- **Scannable** — bullets and short paragraphs, no walls of text
- **Bold** for key terms, `code` for formulas/code
- No markdown tables on platforms that don't render them — use bullet lists instead
- **Message chunking** — deliver lessons as 3-4 short messages (~150 words each), not one giant message. See `skills/tutor/references/lesson-delivery.md` for chunking rules and per-format sequences.
- **Emoji anchors** — use the 4 structural anchors (📖🧠💡✏️) at the start of each message. No decorative emoji. See lesson-delivery.md for the full anchor system.
- **Slack** — full markdown works
- **Telegram** — use HTML formatting (`<b>`, `<i>`, `<code>`, `<pre>`). See `openclaw/SOUL.md` for the Telegram HTML reference.
- **WhatsApp** — plain text preferred, avoid markdown tables
```

This replaces the old "Emoji sparingly -- one or two per message max. Mirror the student's style" line and the old "Telegram -- bold and bullets, keep it simple" line with references to the new systems.
  </action>
  <verify>
    <automated>grep "📖🧠💡✏️" skills/tutor/SKILL.md && grep "📖🧠💡✏️" workspace/AGENTS.md && grep "lesson-delivery.md" skills/tutor/SKILL.md | grep -c "anchor" && grep -c "Message chunking" workspace/AGENTS.md && grep "Telegram" workspace/AGENTS.md | grep -c "HTML" && grep -c "Emoji sparingly" skills/tutor/SKILL.md && grep -c "Emoji sparingly" workspace/AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains the string "📖🧠💡✏️" (the 4 anchor emoji together)
    - SKILL.md contains the string "structural message markers"
    - SKILL.md contains "lesson-delivery.md" in the emoji anchor line
    - SKILL.md does NOT contain the string "Emoji sparingly"
    - AGENTS.md contains the string "📖🧠💡✏️" (the 4 anchor emoji together)
    - AGENTS.md contains the string "Message chunking"
    - AGENTS.md contains the string "150 words"
    - AGENTS.md contains the string "Emoji anchors"
    - AGENTS.md contains "lesson-delivery.md" reference
    - AGENTS.md contains "HTML formatting" in the Telegram line
    - AGENTS.md contains "openclaw/SOUL.md" reference in the Telegram line
    - AGENTS.md does NOT contain the string "Emoji sparingly"
    - AGENTS.md does NOT contain the old string "bold and bullets, keep it simple" for Telegram
  </acceptance_criteria>
  <done>
    SKILL.md Tone & Style section references the 4-anchor system with a link to lesson-delivery.md (replacing "emoji sparingly" guidance). AGENTS.md Platform Formatting section references chunking rules and anchor system from lesson-delivery.md, and points to openclaw/SOUL.md for Telegram-specific HTML formatting. All old conflicting guidance removed from both files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill file -> Agent runtime | Agent interprets prose instructions at runtime. Malformed HTML tag examples could cause rendering issues. |
| User input -> HTML message | Agent constructs HTML messages that may include student-provided text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | openclaw/SOUL.md HTML guidance | accept | HTML tag examples are documentation, not executable code. Telegram Bot API auto-escapes entities. Risk is informational only. |
| T-01-04 | Tampering | User input in HTML messages | mitigate | Skill instructions should NOT recommend raw embedding of user replies into HTML tags. Telegram Bot API auto-escapes most entities when using parseMode=HTML, but skill instructions avoid suggesting patterns like `<b>${userInput}</b>`. The formatting guidance documents agent-generated content only. |
| T-01-05 | Information Disclosure | openclaw/SOUL.md, SKILL.md, AGENTS.md | accept | Files contain no secrets, no PII. They are instructional prose in version control. |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c "📖🧠💡✏️" skills/tutor/SKILL.md` returns 1
2. `grep -c "📖🧠💡✏️" workspace/AGENTS.md` returns 1
3. `grep -c "## Telegram HTML Formatting" openclaw/SOUL.md` returns 1
4. `grep -c "📌" openclaw/SOUL.md` returns 0 (old anchor removed)
5. `grep -c "🔬" openclaw/SOUL.md` returns 0 (old anchor removed)
6. `grep -c "⚡" openclaw/SOUL.md` returns 0 (old anchor removed)
7. `grep -c "Emoji sparingly" skills/tutor/SKILL.md workspace/AGENTS.md` returns 0 (old guidance removed from both)
8. `grep -c "lesson-delivery.md" openclaw/SOUL.md` returns at least 1 (cross-reference present)
9. `grep -c "4,096" openclaw/SOUL.md` returns 1 (character limit documented)
10. All existing non-formatting sections in openclaw/SOUL.md are preserved (How You Teach, Tone, Scheduling, Memory, Resources, Interactivity)
</verification>

<success_criteria>
- openclaw/SOUL.md has Telegram-specific HTML formatting guidance (FMT-02) and 4-anchor system (FMT-03 platform-specific)
- All three files (openclaw/SOUL.md, SKILL.md, AGENTS.md) reference lesson-delivery.md as the canonical source for chunking and anchors
- No file contains conflicting emoji guidance (old "sparingly" or old 6-anchor system)
- Non-formatting content in all files is preserved (teaching method, identity, memory, buttons, quizzes)
</success_criteria>

<output>
After completion, create `.planning/phases/01-message-formatting-chunking/01-02-SUMMARY.md`
</output>
